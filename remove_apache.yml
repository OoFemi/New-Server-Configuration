---

- hosts: all
  become: true
  tasks:


  - name: remove repository index
    apt:
      update_cache: yes
      state: absent

  - name: install apache2 package
    apt:
      name: apache2
      state: absent

  - name: remove php supports for apache
    apt:
      name: libapache2-mod-php
      state: absent
